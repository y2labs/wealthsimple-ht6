# import Pet from './generated/prisma.graphql'
# import UserAchievement from './generated/prisma.graphql'

type Query {
  viewer: Viewer
}

type Viewer {
  token: String
  me: User
}

type User {
  id: ID!
  token: String!

  phoneNumber: String
  email: String
  name: String

  achievements: [UserAchievement!]!
  pet: Pet
}
